import { shorten } from '../../../helpers/shorten';
import { module, test } from 'qunit';

module('Unit | Helper | shorten');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shorten([42]);
  assert.ok(result);
});
